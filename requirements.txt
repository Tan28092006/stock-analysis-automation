# Core data / numerics
pandas>=2.2,<3
numpy>=1.26,<2
scipy>=1.10
python-dotenv>=1.0

# ML — win-probability (LightGBM + isotonic) + legacy ensemble
scikit-learn>=1.4
lightgbm>=4.6
xgboost>=3.2
catboost>=1.2
# shap>=0.50 requires numpy>=2, which conflicts with the numpy<2 pin above (verified
# against PyPI metadata). 0.49.1 has no hard numpy floor and resolves cleanly.
shap>=0.46,<0.50

# Optimizer (legacy pipeline). schemas.py uses stdlib dataclasses, not pydantic.
optuna>=4.0

# Market data
vnstock>=4.0.4
yfinance>=1.4.0
requests>=2.31

# The dashboard/API uses the Python stdlib http.server (no FastAPI/uvicorn needed).
# Optional — foreign-flow harvest (Vietstock, headless browser). Not required to run
# the dashboard; install only if you re-enable the foreign accumulator scrapers:
#   playwright>=1.40   ; then: python -m playwright install chromium
