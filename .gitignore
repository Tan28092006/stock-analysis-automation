__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.env
.env.*
!.env.example

logs/
data/raw/prices/*.csv
data/interim/*
data/processed/*
data/features/*
data/models/*
data/reports/*

!data/raw/
!data/raw/prices/
!data/interim/
!data/processed/
!data/features/
!data/models/
!data/reports/

# The P(win) MR model IS shipped (baked into the Docker image / served on Render) — the
# dashboard's win-probability column is dead without it. All other models stay ignored.
!data/models/win_prob_mr.pkl

# Vietstock login session (auth cookies) - never commit
data/raw/foreign/vietstock_session.json

# Foreign AI-tooling config (not part of this project)
.agents/
.codex-plugin/

# Throwaway experiment outputs / logs at repo root
/backtest_results.json
/bt_30.json
/bt_report.json
/opt_all.json
/optimize_test.json
/optimize_test_widen.json
data/pipeline/eod_update.log
